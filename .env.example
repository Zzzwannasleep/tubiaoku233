# =========================
# PicStoreJson env example
# =========================
# 使用方式：
# 1) 本地：复制为 .env，然后把下面的空值补全
# 2) Vercel：到 Project -> Settings -> Environment Variables，把每一项按 KEY=VALUE 填进去（不要把这整个文件当成变量）

# -------------------------
# 必填：GitHub Gist（icons.json）
# -------------------------
# 你需要先创建一个 Gist（建议 Secret/Private），里面放一个文件：icons.json
# icons.json 内容示例：
# {
#   "name": "Forward",
#   "description": "",
#   "icons": []
# }
#
# GIST_ID：Gist URL 最后一段（例如 https://gist.github.com/<user>/<GIST_ID>）
GIST_ID=
# GITHUB_USER：Gist 所属的 GitHub 用户名
GITHUB_USER=
# GITHUB_TOKEN：GitHub Token（需要能写 Gist）
# 获取位置：GitHub -> Settings -> Developer settings -> Personal access tokens
# - classic token：勾选 gist
# - fine-grained token：勾选 Gists: Read and write
GITHUB_TOKEN=

# -------------------------
# 通用：上传服务
# PICGO / IMGURL / PICUI / GITHUB
# -------------------------
# 说明：
# - PICUI：功能最完整（支持 /manage 管理后台）
# - GITHUB：使用 GitHub Repo 当图床（支持 square/circle/transparent 分类文件夹 + 对应 Gist 分流）
UPLOAD_SERVICE=PICUI

# -------------------------
# GitHub Repo 图床模式（UPLOAD_SERVICE=GITHUB）
# 仓库建议公开，否则 RAW/JSDELIVR 可能无法直接外链访问
# -------------------------
# 创建一个专门的仓库（建议公开），用于存放图片，例如：yourname/pic-bed
# 推荐二选一：
GITHUB_REPO=owner/repo
# 或者：
GITHUB_REPO_OWNER=
GITHUB_REPO_NAME=

# 分支名（默认 main）
GITHUB_REPO_BRANCH=main
# 图片存放根目录（默认 images），最终会写入：images/square、images/circle、images/transparent
GITHUB_REPO_DIR=images

# Repo Token（需要对仓库有 Contents 写权限；留空则复用 GITHUB_TOKEN）
# 获取位置：GitHub -> Settings -> Developer settings -> Personal access tokens
# 建议用 fine-grained token：只授权到这个仓库，并开启 Contents: Read and write
GITHUB_REPO_TOKEN=

# 外链模式：RAW / JSDELIVR / PREFIX
# - RAW：直链 raw.githubusercontent.com（仓库需公开）
# - JSDELIVR：走 jsDelivr CDN（部分地区可能不稳定）
# - PREFIX：自定义前缀（例如 GitHub Pages 或自建 CDN），需配合下方 GITHUB_REPO_URL_PREFIX
GITHUB_REPO_URL_MODE=RAW
# 当 URL_MODE=PREFIX 时使用（例如 GitHub Pages 或自建 CDN 前缀）
# 示例：https://<user>.github.io/<repo>/  （可带/不带结尾斜杠）
GITHUB_REPO_URL_PREFIX=

# commit message 模板（可用 {filename}）
GITHUB_REPO_COMMIT_MESSAGE=Upload {filename}

# GitHub 模式：不同分类文件夹写入不同 Gist 文件名（可选）
# - 这是同一个 Gist 里的不同“文件名”，不是 URL
# - 如果你想 3 个分类都写回同一个 icons.json：把下面 3 个都改成 icons.json
GITHUB_GIST_FILE_SQUARE=icons-square.json
GITHUB_GIST_FILE_CIRCLE=icons-circle.json
GITHUB_GIST_FILE_TRANSPARENT=icons-transparent.json

# -------------------------
# PICUI 模式（UPLOAD_SERVICE=PICUI）
# -------------------------
# 获取方式：登录 picui.cn -> 个人中心/设置 -> API Token（按网站提示生成）
PICUI_TOKEN=
# 0=私有 1=公开（默认 1）
PICUI_PERMISSION=1
# 可选：策略/相册/过期时间（在 PICUI 后台创建后填对应 ID）
PICUI_STRATEGY_ID=
PICUI_ALBUM_ID=
PICUI_EXPIRED_AT=

# -------------------------
# PicGo 模式（UPLOAD_SERVICE=PICGO）
# -------------------------
# 获取方式：picgo.net 账号后台 -> API Key（按网站提示生成）
PICGO_API_KEY=

# -------------------------
# ImgURL 模式（UPLOAD_SERVICE=IMGURL）
# -------------------------
# 获取方式：imgurl.org 账号后台 -> UID / Token（按网站提示生成）
IMGURL_API_UID=
IMGURL_API_TOKEN=
# 备注：当前代码未使用该变量，先预留
IMGURL_ALBUM_ID=

# -------------------------
# AI 抠图（可选）
# -------------------------
# Clipdrop：到 clipdrop.co 的 API 页面创建 key
CLIPDROP_API_KEY=
# remove.bg：到 remove.bg 的 API 页面创建 key
REMOVEBG_API_KEY=

# -------------------------
# 自定义 AI 抠图（可选）
# -------------------------
# CUSTOM_AI_ENABLED=1 后，前端需要输入 CUSTOM_AI_PASSWORD 才能使用自定义接口
CUSTOM_AI_ENABLED=0
CUSTOM_AI_PASSWORD=
# 你的自定义抠图接口 URL（POST multipart/form-data）
CUSTOM_AI_URL=
# 文件字段名（默认 image）
CUSTOM_AI_FILE_FIELD=image
# 可选：鉴权 key（会按下面 header/prefix 拼接）
CUSTOM_AI_API_KEY=
CUSTOM_AI_AUTH_HEADER=Authorization
CUSTOM_AI_AUTH_PREFIX=

# -------------------------
# 管理后台 /manage（可选）
# -------------------------
# 开启：ADMIN_ENABLED=1
ADMIN_ENABLED=0
# 管理后台登录密码（建议设置强密码）
ADMIN_PASSWORD=
ADMIN_COOKIE_MAX_AGE=86400

# -------------------------
# 其他
# -------------------------
# 强烈建议设置随机长字符串（用于签名 Cookie / HttpOnly 登录态）
# 生成方式示例（本地）：python -c "import secrets; print(secrets.token_urlsafe(32))"
FLASK_SECRET_KEY=

# 二次元随机背景 API（可选）
# 你也可以换成自己的随机图接口
RANDOM_BG_API=https://api.btstu.cn/sjbz/?lx=dongman
